# Cartesi utility sources

`base64.cpp`, `base64.hpp`, `hex.cpp`, `hex.hpp`, and `concepts.hpp` are
vendored from the corresponding files under `machine-emulator/src`. The
machine-emulator copies are the source of truth and these files must remain
synchronized with them.
